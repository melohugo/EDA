#include <stdio.h>
#include <limits.h>
#include <string.h>
#include <ctype.h>
#include <math.h>
#define show(n) printf("%d\n", n);
#define entrou printf("ENTROU\n");
#define swap(c1, c2) {int c = c1; c1 = c2; c2 = c;}
#define MAIN main

int conta_par(char *str, int *pares) 
{
	if (*str != '\0')

}

int MAIN(void)
{
	char str[250];
	int pares;

	scanf("%s", str);	
	c = conta_par(str, &pares);
	return 0;
}
